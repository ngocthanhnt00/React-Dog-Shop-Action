import React from 'react'

const DogCard = (props) => {
    const { id, name, breed, desciption, price, imageUrl } = props;

    return (
        <>

        </>
    )
}

export default DogCard
